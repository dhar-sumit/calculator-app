from setuptools import setup, find_packages

setup(
    name='calculator_app',
    version='0.1',
    description='A simple calculator app for addition, subtraction, and multiplication',
    author='Sumit Dhar',
    packages=find_packages(),  # Automatically find your modules
    python_requires='>=3.6',
)
